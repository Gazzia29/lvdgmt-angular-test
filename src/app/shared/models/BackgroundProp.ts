export interface BackgroundProp {
  imgName: string; // "tree.png"
  zIndex: number; // 150
  xPos?: number;
  yPos?: number; // 0;
  xFrom?: string; // "left"
  yFrom?: string; // "bottom"
  wFactor?: string; // "w"
  hFactor?: string; // "h"
  w?: number; // 100;
  h?: number; // 100;
}
